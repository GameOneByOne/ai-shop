import{ContentStudio}from"@/components/content/content-studio";import{listListingPackages,listSkus}from"@/lib/data/store";import{reviewListingPackage,updateListingChecks}from"./actions";
const imageLabels:Record<string,string>={unverified:"未核验",authorized:"供应商已授权",self_shot:"自有实拍",rejected:"不可使用"};const complianceLabels:Record<string,string>={pending:"待检查",passed:"已通过",needs_changes:"需要修改",rejected:"不合规"};const reviewLabels:Record<string,string>={draft:"草稿",pending_review:"待人工审核",approved:"已批准",rejected:"已驳回"};
export default async function Page(){const[skus,packages]=await Promise.all([listSkus(),listListingPackages()]);return <><header className="top"><div><div className="eyebrow">Listing Production</div><h1>上架内容制作</h1><p className="muted">1688 原始资料经过标题、卖点、详情页、图片授权和合规审核后，才能进入待上架。</p></div></header><div className="notice">AI 只生成草稿，不得虚构材质、功能、检测证明或售后承诺；正式上架必须人工批准。</div><ContentStudio skus={skus}/><div className="section-heading"><div><h2>上架资料包</h2><p>图片授权、合规检查与最终审批</p></div></div>{packages.length?<div className="listing-package-list">{packages.map(item=><article className="card listing-package" key={item.id}><div><span className="eyebrow">{item.skuCode??"未绑定 SKU"}</span><h2>{item.selectedTitle??item.title}</h2><p className="muted">{item.categoryPath??"类目待确认"} · {new Date(item.createdAt).toLocaleString("zh-CN")}</p></div><div className="package-status"><span>图片：{imageLabels[item.imageAuthorizationStatus]??item.imageAuthorizationStatus}</span><span>合规：{complianceLabels[item.complianceStatus]??item.complianceStatus}</span><span>审核：{reviewLabels[item.reviewStatus]??item.reviewStatus}</span></div><form action={updateListingChecks} className="package-checks"><input type="hidden" name="id" value={item.id}/><select className="input" name="image_authorization_status" defaultValue={item.imageAuthorizationStatus}><option value="unverified">图片未核验</option><option value="authorized">供应商已授权</option><option value="self_shot">自有实拍</option><option value="rejected">图片不可使用</option></select><select className="input" name="compliance_status" defaultValue={item.complianceStatus}><option value="pending">合规待检查</option><option value="passed">合规已通过</option><option value="needs_changes">合规需要修改</option><option value="rejected">内容不合规</option></select><button className="secondary-btn">保存检查</button></form><div className="package-actions"><form action={reviewListingPackage}><input type="hidden" name="id" value={item.id}/><input type="hidden" name="decision" value="rejected"/><button className="secondary-btn">驳回修改</button></form><form action={reviewListingPackage}><input type="hidden" name="id" value={item.id}/><input type="hidden" name="decision" value="approved"/><button className="btn">人工批准</button></form></div></article>)}</div>:<div className="empty-state">还没有上架资料包。选择 SKU 后生成第一份内容草稿。</div>}</>}
