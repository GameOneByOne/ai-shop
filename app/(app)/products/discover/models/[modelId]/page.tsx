import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";export default async function Page({params}:{params:Promise<{modelId:string}>}){const{modelId}=await params,db=await createClient(),{data:run}=await db.from("sourcing_runs").select("id").eq("provider","1688-browser").order("created_at",{ascending:false}).limit(1).maybeSingle();if(!run)redirect("/products/discover");redirect(`/products/discover/runs/${run.id}/models/${modelId}`)}
