import{StoreGoals}from"@/components/settings/store-goals";export default function Page(){const supabase=Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL&&process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);const ai=Boolean(process.env.OPENAI_API_KEY&&process.env.OPENAI_MODEL);return <><header className="top"><div><div className="eyebrow">Strategy & Production</div><h1>设置</h1><p className="muted">定义 AI 店长的目标函数，并检查生产配置。</p></div></header><StoreGoals/><div className="grid settings-status"><div className="card"><h2>Supabase</h2><span className={`health ${supabase?"good":"warning"}`}>{supabase?"已配置":"未配置"}</span><p className="muted">数据库、认证和 RLS 数据隔离</p></div><div className="card"><h2>AI 服务</h2><span className={`health ${ai?"good":"warning"}`}>{ai?"已配置":"未配置"}</span><p className="muted">DeepSeek 服务端模型</p></div></div></>}
