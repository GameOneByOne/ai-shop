import Link from "next/link";
import {deleteSku,saveSku} from "./actions";
import {listSkus} from "@/lib/data/store";
const labels:Record<string,string>={DISCOVERED:"已发现",SOURCING:"找货中",CANDIDATE:"候选",READY:"待上架",TESTING:"测试销售",GROWING:"增长",STABLE:"稳定",DECLINING:"衰退",CLEARANCE:"清仓",STOPPED:"停止经营"};
export default async function Page(){const items=await listSkus();return <><header className="top"><div><h1>SKU</h1><p className="muted">从发现到停止经营的完整商品生命周期</p></div></header><form action={saveSku} className="card crud-form"><h2>新增 SKU</h2><input className="input" name="code" placeholder="SKU 编码" required/><input className="input" name="name" placeholder="商品名称" required/><input className="input" name="category" placeholder="品类" required/><input className="input" name="price" type="number" step="0.01" placeholder="售价"/><input className="input" name="cost" type="number" step="0.01" placeholder="可持续到手成本"/><input className="input" name="stock" type="number" placeholder="库存"/><select className="input" name="lifecycle_status" defaultValue="DISCOVERED">{Object.entries(labels).map(([value,label])=><option value={value} key={value}>{label}</option>)}</select><button className="btn">保存</button></form><div className="card table-wrap"><table className="table"><thead><tr><th>SKU</th><th>售价</th><th>到手成本</th><th>库存</th><th>生命周期</th><th>操作</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><Link href={`/skus/${x.id}`}>{x.code} · {x.name}</Link></td><td>¥{x.price}</td><td>¥{x.cost}</td><td>{x.stock}</td><td><span className={`lifecycle ${x.lifecycleStatus.toLowerCase()}`}>{labels[x.lifecycleStatus]??x.lifecycleStatus}</span></td><td><form action={deleteSku}><input type="hidden" name="id" value={x.id}/><button className="text-btn">删除</button></form></td></tr>)}</tbody></table></div></>}
