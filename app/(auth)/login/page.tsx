import{login,signup}from"./actions";
export default async function Page({searchParams}:PageProps<"/login">){const{error,message}=await searchParams;return <main className="main" style={{maxWidth:440,margin:"80px auto"}}><div className="card"><div className="eyebrow">AI Store Manager</div><h1>登录 AI 店长</h1><p className="muted">使用 Supabase Email 账户</p>{error&&<div className="status-box error">{error==="missing"?"请填写邮箱和密码":error==="signup_invalid"?"请输入有效邮箱和至少 8 位密码":error}</div>}{message&&<div className="status-box success">注册成功，请检查邮箱确认账户后登录。</div>}<form><label>邮箱<input className="input" name="email" type="email" required autoComplete="email"/></label><br/><br/><label>密码<input className="input" name="password" type="password" minLength={8} required autoComplete="current-password"/></label><br/><br/><div className="auth-actions"><button className="btn" formAction={login}>登录</button><button className="secondary-btn" formAction={signup}>注册</button></div></form></div></main>}
