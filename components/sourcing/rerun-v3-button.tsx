"use client";import{useState}from"react";export function RerunV3Button(){const[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function run(){setBusy(true);setMessage("");try{const response=await fetch("/api/sourcing/ai-selection",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({runId:location.pathname.match(/runs\/([^/]+)/)?.[1]})}),body=await response.json();if(!response.ok)throw new Error(body.error??"重新评估失败");location.reload()}catch(e){setMessage(e instanceof Error?e.message:"重新评估失败")}finally{setBusy(false)}}return <div className="action-buttons"><button className="secondary-btn" disabled={busy} onClick={()=>void run()}>{busy?"正在评估…":"重新运行货源评估"}</button>{message&&<span className="status-box error">{message}</span>}</div>}
