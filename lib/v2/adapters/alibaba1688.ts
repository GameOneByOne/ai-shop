import type{PurchaseOrder,Shipment,SourceSku}from"../domain";
export interface Alibaba1688Adapter{getSku(id:string):Promise<SourceSku|null>;checkInventory(id:string,q:number):Promise<{available:boolean;inventory:number}>;createPurchaseOrder(input:{orderId:string;sourceSku:SourceSku;quantity:number}):Promise<PurchaseOrder>;simulateShipment(po:PurchaseOrder):Promise<Shipment>}
export class MockAlibaba1688Adapter implements Alibaba1688Adapter{constructor(private skus:SourceSku[]){}async getSku(id:string){return this.skus.find(x=>x.id===id)??null}async checkInventory(id:string,q:number){const x=await this.getSku(id);return{available:Boolean(x&&x.inventory>=q&&x.status==="ACTIVE"),inventory:x?.inventory??0}}async createPurchaseOrder({orderId,sourceSku,quantity}:{orderId:string;sourceSku:SourceSku;quantity:number}):Promise<PurchaseOrder>{return{id:`po-${Date.now()}`,orderId,sourceSkuId:sourceSku.id,status:"SUPPLIER_PROCESSING",actualPurchaseCost:sourceSku.purchasePrice*quantity,actualShippingCost:sourceSku.shippingFee,platformOrderNo:`MOCK1688${Date.now()}`,createdAt:new Date().toISOString()}}async simulateShipment(po:PurchaseOrder):Promise<Shipment>{return{id:`ship-${Date.now()}`,orderId:po.orderId,purchaseOrderId:po.id,carrier:"圆通",trackingNumber:`MOCK${Date.now()}`,status:"SHIPPED",createdAt:new Date().toISOString()}}}
