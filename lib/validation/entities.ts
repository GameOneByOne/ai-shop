import {z} from "zod";
export const candidateProductSchema=z.object({name:z.string().min(1).max(200),category:z.string().min(1),estimated_cost:z.number().nonnegative(),estimated_price:z.number().positive(),status:z.enum(["candidate","researching","sample","approved","rejected"])});
export const dailyMetricSchema=z.object({date:z.iso.date(),sku_code:z.string().min(1),impressions:z.number().int().nonnegative(),clicks:z.number().int().nonnegative(),visitors:z.number().int().nonnegative(),favorites:z.number().int().nonnegative(),add_to_cart:z.number().int().nonnegative(),orders:z.number().int().nonnegative(),units_sold:z.number().int().nonnegative(),revenue:z.number().nonnegative(),refund_orders:z.number().int().nonnegative(),refund_amount:z.number().nonnegative(),ad_spend:z.number().nonnegative()});
